query Posts($pagination: Pagination!) {
  posts(pagination: $pagination) {
    id
    creationTime
    title
    contents
    category
    commentCount
    # imageUrls
    user {
      id
      nickname
    }
  }
}
