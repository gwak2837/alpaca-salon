query Posts($pagination: Pagination!) {
  posts(pagination: $pagination) {
    id
    creationTime
    modificationTime
    title
    contents
    category
    user {
      id
      nickname
    }
  }
}
